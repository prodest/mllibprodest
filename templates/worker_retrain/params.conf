# -----------------------------------------------------------------------------------------------------------------
# Arquivo de configuração dos modelos da classe ModeloRETRAIN. Instruções de preenchimento:
# 
# - Coloque o nome do modelo entre colchetes e sem espaços (este nome será utilizado para publicá-lo);
# - Não utilize aspas nem espaços nos valores dos parâmetros;
# - source_file: Nome do arquivo com o código fonte da implementação do modelo, sem a extensão '.py';
# - model_class: Nome da classe do modelo. Mantenha o padrão ModeloRETRAIN;
# - experiment_name: Nome do experimento criado no MLflow ao registrar o modelo;
# - model_provider_name: Nome do provedor do modelo. Mantenha o padrão mlflow;
# - dataset_provider_name: Nome do provedor dos datasets.
#
# Obs.: Crie uma seção para cada modelo a ser publicado. As seções devem estar separadas por uma linha em branco.
# -----------------------------------------------------------------------------------------------------------------

[NOME_DO_MODELO]
source_file = retrain1
model_class = ModeloRETRAIN
experiment_name = nome_do_experimento
model_provider_name = mlflow
dataset_provider_name = minio
